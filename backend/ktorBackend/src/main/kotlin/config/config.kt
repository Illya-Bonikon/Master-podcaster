package com.example.config

object JwtConfig {
    const val secret = "secret"
    const val audience = "web-client"
    const val domain = "http://localhost:8080/"
    const val realm = "ktor sample app"
}
